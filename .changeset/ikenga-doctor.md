---
"@ikenga/cli": minor
---

Add `ikenga doctor [--fix]` — a bridge-first health check for package installs. Detects broken/orphaned installs by driving the running shell's iyke health routes, and optionally repairs them with `--fix`.
